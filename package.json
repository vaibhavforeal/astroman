{
  "name": "astroman",
  "version": "1.0.0",
  "description": "Vedic astrology + numerology consultation tool with a GPT-style chat interface. Computes a sidereal (Lahiri) birth chart with Swiss Ephemeris and interprets it with Claude.",
  "main": "server/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "sweph": "latest"
  }
}
